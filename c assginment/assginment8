#include<stdio.h>
int main()
{
int num;
printf(enter an integer you want to check:);
fflush(stdin);fflush(stdout);
scanf(%d,&num);
if (num%2==0)
		printf(%d is enteger,num);
   else
	printf(%d is odd,num);
return 0;
}
